package gui;

import javax.swing.*;

public class Entrance extends JPanel implements Runnable{

    public Entrance(){
        setLayout(null);
    }

    @Override
    public void run() {
        // We'll write here the part that we want to run in another thread. (Game)
    }
}
